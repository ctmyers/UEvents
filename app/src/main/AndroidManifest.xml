<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.uevents.app.uevents"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCm1duFJRHh1oOckm6McwaWrM5bCMZwmOI"/>

        <activity
            android:name=".ListViewEventActivity"
            android:label="@string/title_activity_list_view_event"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".SearchActivity">
        </activity>

        <activity android:name=".CreateEventActivity"></activity>
        <activity android:name=".AttendingActivity"></activity>
        <activity android:name=".MyEventsActivity"></activity>
    </application>

</manifest>